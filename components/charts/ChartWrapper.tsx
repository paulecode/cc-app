export type ChartWrapperProps = {
  ChartTitle: string
}

export const ChartWrapper: React.FC<({ children: React.ReactNode, props: ChartWrapperProps })> = ({ children, props }) => {
  return <div>{ }</div>
}

